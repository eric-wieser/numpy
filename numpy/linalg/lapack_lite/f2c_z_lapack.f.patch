@@ -15277,9 +15277,10 @@
                V( I, I ) = ONE
                IF( LSAME( STOREV, 'C' ) ) THEN
 !                 Skip any trailing zeros.
+                  LASTV = N
-                  DO LASTV = N, I+1, -1
-                     IF( V( LASTV, I ).NE.ZERO ) EXIT
-                  END DO
+C                 DO LASTV = N, I+1, -1
+C                    IF( V( LASTV, I ).NE.ZERO ) EXIT
+C                 END DO
                   J = MIN( LASTV, PREVLASTV )
 *
 *                 T(1:i-1,i) := - tau(i) * V(i:j,1:i-1)' * V(i:j,i)
@@ -15289,9 +15290,10 @@
      $                        ZERO, T( 1, I ), 1 )
                ELSE
 !                 Skip any trailing zeros.
+                  LASTV = N
-                  DO LASTV = N, I+1, -1
-                     IF( V( I, LASTV ).NE.ZERO ) EXIT
-                  END DO
+C                 DO LASTV = N, I+1, -1
+C                    IF( V( I, LASTV ).NE.ZERO ) EXIT
+C                 END DO
                   J = MIN( LASTV, PREVLASTV )
 *
 *                 T(1:i-1,i) := - tau(i) * V(1:i-1,i:j) * V(i,i:j)'
@@ -15337,9 +15339,10 @@
                      VII = V( N-K+I, I )
                      V( N-K+I, I ) = ONE
 !                    Skip any leading zeros.
+                     LASTV = 1
-                     DO LASTV = 1, I-1
-                        IF( V( LASTV, I ).NE.ZERO ) EXIT
-                     END DO
+C                    DO LASTV = 1, I-1
+C                       IF( V( LASTV, I ).NE.ZERO ) EXIT
+C                    END DO
                      J = MAX( LASTV, PREVLASTV )
 *
 *                    T(i+1:k,i) :=
@@ -15353,9 +15356,10 @@
                      VII = V( I, N-K+I )
                      V( I, N-K+I ) = ONE
 !                    Skip any leading zeros.
+                     LASTV = 1
-                     DO LASTV = 1, I-1
-                        IF( V( I, LASTV ).NE.ZERO ) EXIT
-                     END DO
+C                    DO LASTV = 1, I-1
+C                       IF( V( I, LASTV ).NE.ZERO ) EXIT
+C                    END DO
                      J = MAX( LASTV, PREVLASTV )
 *
 *                    T(i+1:k,i) :=
